version: "3.9"

services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./neovolt.db:/app/../neovolt.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
